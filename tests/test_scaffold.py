"""Placeholder so `uv run pytest` is green on a fresh scaffold.

Delete this once the first real test exists. A suite that passes because it
asserts nothing is worse than no suite: it reports success nobody earned.
"""


def test_scaffold_is_wired():
    assert True, "replace me with a test of a checker, e.g. validate_solvability()"
